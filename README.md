# MP-HGM: MetaPath-based Heterogeneous Graph Matching

## 项目简介

MP-HGM 是一个基于元路径的异构图匹配模型，使用 PyTorch 和 PyTorch Geometric 实现。该模型通过编码节点特征和元路径特征，并使用跨图匹配模块计算图之间的相似度。

## 安装依赖

请确保已安装以下依赖项：

- Python 3.8+
- PyTorch 2.0+
- CUDA 12.1 (recommended)
- NVIDIA GPU with ≥12GB memory
